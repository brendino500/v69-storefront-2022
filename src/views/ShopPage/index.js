import Title from '@/components/Title'
import SectionWrapper from '@/components/SectionWrapper'

function ShopPage() {
  return (
    <SectionWrapper className="h-[600px] flex items-center justify-center flex-col">
      <Title element="h1">SHOPIFY PAGE</Title>
      <p>Todo:</p>
      <p>• Link to shopify account</p>
      <p>• Populate items via shopify</p>
    </SectionWrapper>
  )
}

export default ShopPage
